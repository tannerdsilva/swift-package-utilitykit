[build-system]
requires = ["setuptools"]
build-backend = "setuptools.backends._legacy:_Backend"

[project]
name = "swift-package-utilitykit"
version = "1.0.0"
description = "Hermes Agent plugin for Swift package inspection and security audit"
requires-python = ">=3.11"
